# Example secret layout for secrets/homelab.yaml.
# Encrypt the real file with SOPS (age). NEVER commit plaintext values.
#   sops --encrypt --in-place secrets/homelab.yaml
git_token: REPLACE_ME            # GitHub token for private Git over HTTPS (-> /run/secrets/git_token)
oauth2_proxy_env: REPLACE_ME     # oauth2-proxy env (cookie secret, GitHub client id/secret)
